syntax = "proto3";

package suggestions;

service SuggestionsService {
    rpc GetSuggestions (SuggestionRequest) returns (SuggestionResponse);
}

message SuggestionRequest {
    string user_id = 1;
    repeated string ordered_items = 2;
}

message Book {
    string book_id = 1;
    string title = 2;
    string author = 3;
}

message SuggestionResponse {
    repeated Book suggestions = 1;
}